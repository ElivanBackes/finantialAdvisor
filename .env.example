MONGO_URI=mongodb://localhost:27017
MONGO_DB_NAME=financial_advisor

# Etapa 2 (coleta de dados) — deixe em branco se ainda não tiver as chaves.
# Sem elas, a respectiva fonte é pulada (isolado, não quebra as outras análises).
# BRAPI_API_TOKEN: a brapi.dev passou a exigir token em toda requisição (mesmo
# consultas simples) — obtenha um gratuito em https://brapi.dev.
BRAPI_API_TOKEN=
NEWSAPI_KEY=

LOG_LEVEL=INFO
